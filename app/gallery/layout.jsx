export const metadata = {
  title: "Gallery",
  description: "Photo highlights from ISAC events at BTU Cottbus-Senftenberg.",
};

export default function GalleryLayout({ children }) {
  return children;
}
